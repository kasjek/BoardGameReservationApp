{
  "name": "boardgame-reservation-app",
  "version": "1.0.0",
  "private": true,
  "description": "BoardGameReservationApp — mobile-first table booking web client",
  "main": "server.js",
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "NODE_ENV=production node server.js",
    "dev": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
